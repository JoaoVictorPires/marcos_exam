function text(mensagem){
    setTimeout((text) => {
        console.log(mensagem)
        
    }, 5000);
}
let mensagem = 'Not avaiable'
console.log(text(mensagem))
// Professor não entendi o porque estar dando undefined, porém está funcionando :).